#include "main.h"
/**
*print_diagonal - draws a diagonal line
*@n: number of time to print
*Return: empty
*/
void print_diagonal(int n)
{
int x, y;
if (n <= 0)
{
_putchar(n);
}
ele
{
for (x = 0; x < n; x++)
{
foe (y = 0; y < x; y++)
{
_putchar(32);
}
_putchar(92);
_putchar(n);
}
}
