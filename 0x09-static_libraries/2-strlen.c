#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
*_strlen - Returns the length of a string
*@s: String to count
*Return: String length
**/

int _strlen(char *s)
{
return (strlen(s));
}

